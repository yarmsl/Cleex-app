import React from 'react';
import { ScrollView } from 'react-native';

const Policy = () => {
    return (
        <ScrollView>
            <Text>123</Text>
        </ScrollView>
    )
}

export default Policy;